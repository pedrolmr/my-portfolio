import React from 'react';

const Header = () => {
    return(
        <header>
            <nav>
                <a>Home</a>
                <a>About</a>
                <a>Projects</a>
                <a>Contact</a>
            </nav>
        </header>
    );
}

export default Header;